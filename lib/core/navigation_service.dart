import 'package:flutter/material.dart';

// Clave global para manejar navegación fuera del contexto (por ejemplo, desde Firebase)
final GlobalKey<NavigatorState> navigatorKey = GlobalKey<NavigatorState>();
